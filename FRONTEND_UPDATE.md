# 前端頁面更新說明

## 更新內容

### 1. 頁面標題更新
- 舊：台語語音轉台羅拼音
- 新：**台語醫療對談語音辨識**

### 2. 新增情境選擇功能
使用者可以在錄音前選擇對話情境：
- 一般醫療對談
- 症狀描述（預設）
- 診斷相關
- 處方用藥
- 醫病諮詢

### 3. 辨識結果顯示改進

#### 之前（單一結果）
```
台羅拼音結果：
[辨識結果]
```

#### 現在（雙結果對照）
```
📝 原始辨識結果：
[Whisper 直接辨識的結果]

✨ 修正後結果：
[GPT 修正流暢性後的結果]

使用情境：症狀描述
```

### 4. 視覺設計改進
- **情境選擇區**：灰色背景區塊，清楚標示
- **修正結果區**：藍色背景高亮顯示，突出強化效果
- **情境標籤**：底部顯示當前使用的情境

## 使用流程

1. **選擇情境**
   - 在下拉選單中選擇適合的對話情境
   - 預設為「症狀描述」

2. **開始錄音**
   - 點擊「🎤 開始錄音」按鈕
   - 允許瀏覽器使用麥克風

3. **說話**
   - 使用台語進行醫療對談

4. **停止錄音**
   - 點擊「⏹️ 停止錄音」

5. **查看結果**
   - 原始辨識：Whisper 的直接辨識結果
   - 修正後結果：GPT 優化流暢性後的結果
   - 可以對照兩者差異

## 技術細節

### API 請求格式
```javascript
FormData {
  audio: Blob,           // 音訊檔案
  context: String        // 情境選項
}
```

### API 回應格式
```json
{
  "success": true,
  "original": "原始辨識文字",
  "enhanced": "修正後文字",
  "context": "symptom"
}
```

## 樣式特點

### 情境選擇器
- 全寬度下拉選單
- hover 時邊框變藍色
- focus 時有藍色陰影效果

### 修正結果框
- 淡藍色背景 (#f0f8ff)
- 藍色邊框 (#667eea)
- 藍色標題文字

### 情境資訊
- 灰色小字
- 上方有分隔線
- 居中顯示

## 啟動方式

```bash
# 啟動 Flask 應用
python3 app.py

# 瀏覽器開啟
http://localhost:5001
```

輸出訊息：
```
============================================================
台語醫療對談語音辨識 - 網頁版
============================================================
正在啟動網頁伺服器...
請在瀏覽器開啟: http://localhost:5001

功能:
  - Whisper 語音辨識
  - GPT 流暢性修正
  - 支援五種醫療對談情境

按 Ctrl+C 停止伺服器
============================================================
```

## 檔案變更

### 修改的檔案
1. `templates/index.html` - 前端頁面
   - HTML 結構更新
   - CSS 樣式新增
   - JavaScript 邏輯修改

2. `app.py` - 後端應用
   - 已更新以支援情境參數
   - 回應格式包含 original 和 enhanced

## 注意事項

1. **瀏覽器相容性**
   - 需要支援 MediaRecorder API
   - 建議使用 Chrome、Edge、Firefox

2. **麥克風權限**
   - 首次使用需授權麥克風權限
   - HTTPS 環境下運作更佳

3. **網路需求**
   - 需要連線到 OpenAI API
   - 處理時間取決於網路速度

4. **隱私保護**
   - 錄音檔案處理後立即刪除
   - 不儲存語音資料

