# 台語醫療對談語音辨識系統

## 概述

本系統使用 OpenAI Whisper 進行台語語音辨識，並透過 GPT-4o 強化醫療專業術語的辨識準確度，特別針對醫療醫院對談場景優化。

## 主要改進

### 1. Whisper 辨識提示
在語音辨識階段加入醫療領域的提示（prompt），幫助 Whisper 更準確識別醫療相關詞彙。

### 2. GPT 強化辨識
使用 GPT-4o 對 Whisper 的辨識結果進行強化，特別針對：
- **醫療術語修正**：身體部位、症狀、疾病名稱、藥物用語
- **台語醫療慣用語**：看病、吃藥、痛等常見表達
- **語境修正**：根據醫療對話邏輯修正可能的辨識錯誤

### 3. 情境化辨識
支援五種醫療對談情境，針對不同場景優化辨識：
- `general` - 一般醫療對談
- `diagnosis` - 診斷相關對話
- `symptom` - 症狀描述對話
- `prescription` - 處方用藥對話
- `consultation` - 醫病諮詢對話

## 使用方式

### 命令列使用

#### 基本用法（使用預設情境）
```bash
python3 taiwanese_speech_to_tailo.py audio.wav
```

#### 指定情境
```bash
# 症狀描述對話
python3 taiwanese_speech_to_tailo.py audio.wav symptom

# 診斷相關對話
python3 taiwanese_speech_to_tailo.py audio.wav diagnosis

# 處方用藥對話
python3 taiwanese_speech_to_tailo.py audio.wav prescription
```

### Web API 使用

啟動 Flask 應用程式：
```bash
python3 app.py
```

API 端點：`POST /api/transcribe`

請求參數：
- `audio`: 音訊檔案（必填）
- `context`: 情境選項（選填，預設 `general`）

範例回應：
```json
{
  "success": true,
  "original": "海產吃了肚子很痛要去看醫生",
  "enhanced": "海產食了，腹肚煞滾絞疼，上好緊去看醫生",
  "context": "symptom"
}
```

## 醫療詞彙對照表

### 常見身體部位
| 台語 | 中文 | 台羅拼音 |
|------|------|----------|
| 腹肚 | 肚子 | pak-tóo |
| 頭殼 | 頭 | thâu-khak |
| 胸坎 | 胸部 | hing-khám |
| 心臟 | 心臟 | sim-tsōng |
| 肝 | 肝 | kuann |

### 常見症狀
| 台語 | 中文 | 台羅拼音 |
|------|------|----------|
| 疼/痛 | 痛 | thiàⁿ |
| 滾絞疼 | 絞痛 | kún-ká-thiànn |
| 暈 | 暈 | hîn |
| 發燒 | 發燒 | huat-sio |
| 咳嗽 | 咳嗽 | ka-sàu |
| 流鼻水 | 流鼻涕 | lâu-phīⁿ-tsuí |

### 常見醫療用語
| 台語 | 中文 | 台羅拼音 |
|------|------|----------|
| 看病 | 看病 | khuànn-pēⁿ |
| 看醫生 | 看醫生 | khuànn-i-sing |
| 醫生館 | 診所 | i-sing-kuán |
| 食藥 | 吃藥 | tsia̍h-io̍h |
| 藥仔 | 藥 | io̍h-á |
| 注射 | 打針 | tsù-siā |

## 輸出格式

### 文字檔案輸出
執行命令後會產生兩個檔案：

1. `{檔名}_medical_transcript.txt` - 可讀的文字格式
```
情境: symptom
======================================================================

原始辨識結果:
海產吃了肚子很痛要去看醫生

強化後結果:
海產食了，腹肚煞滾絞疼，上好緊去看醫生
```

2. `{檔名}_medical_transcript.json` - JSON 格式
```json
{
  "original": "海產吃了肚子很痛要去看醫生",
  "enhanced": "海產食了，腹肚煞滾絞疼，上好緊去看醫生",
  "context": "symptom"
}
```

## 系統優勢

1. **雙層辨識**：Whisper + GPT 雙重處理，提高準確度
2. **醫療專業化**：針對醫療術語優化，減少專業詞彙辨識錯誤
3. **情境化處理**：根據對話場景調整辨識策略
4. **台語保真**：保持台語口語的自然表達方式
5. **可追溯性**：同時提供原始和強化結果，方便比對驗證

## 應用場景

1. **醫療記錄**：自動記錄醫病對談內容
2. **遠距醫療**：輔助遠距問診的語音轉文字
3. **醫療研究**：建立台語醫療對話語料庫
4. **輔助診斷**：協助醫師快速了解病患症狀描述
5. **病歷建檔**：自動化台語病歷文字記錄

## 注意事項

1. **環境變數**：需要設定 `OPENAI_API_KEY` 環境變數
2. **網路連線**：需要穩定的網路連線以使用 OpenAI API
3. **API 費用**：使用 Whisper 和 GPT-4o 會產生 API 費用
4. **隱私保護**：醫療對話包含敏感資訊，請注意資料安全
5. **辨識限制**：複雜的醫學術語或罕見疾病名稱可能需要人工校正

## 技術細節

### 辨識流程
```
音訊檔案
  ↓
Whisper 辨識（含醫療提示）
  ↓
原始中文文字
  ↓
GPT-4o 強化（醫療術語修正）
  ↓
強化後的中文文字
```

### 溫度參數
- Whisper: 預設值
- GPT-4o: 0.2（較低溫度提高準確性）

### Token 限制
- 最大輸出 token: 2000

## 未來改進方向

1. **整合台羅字典**：結合 `tailo_dictionary.py` 進行更精確的詞彙對照
2. **即時辨識**：支援即時串流音訊辨識
3. **多語言支援**：支援台語、華語混合對話
4. **自訂醫療詞庫**：允許使用者新增特定醫療機構的慣用詞彙
5. **辨識信心度**：提供每個詞彙的辨識信心度分數

